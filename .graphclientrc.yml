sources:
  - name: luro
    handler:
      graphql:
        endpoint: ${PUBLIC_LUCKY_ROUND_GRAPH_URL}
        operationHeaders:
          Authorization: Bearer ${PUBLIC_THYME_TOKEN}
        schemaHeaders:
          Authorization: Bearer ${PUBLIC_THYME_TOKEN}

codegen:
  scalars:
    BigInt: bigint
    Bytes: string

documents:
  - ./src/schema.graphql